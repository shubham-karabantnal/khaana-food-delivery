# This file is used during `vite build` for production deployments.
# VITE_API_URL should point to your deployed backend URL on Vercel.
# Update this (or set it as an env var in Vercel dashboard) after deploying the backend.
VITE_API_URL=https://khaana-backend.vercel.app
